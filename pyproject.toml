[project]
name = "alqueries"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11,<3.13"
dependencies = [
    "datasets>=3.0.0",
    "numpy>=2.4.4",
    "pillow>=10.0.0",
    "scikit-learn>=1.8.0",
    "torch>=2.11.0",
    "transformers>=4.40.0",
]

[tool.uv]
package = true

[dependency-groups]
dev = [
    "pytest>=9.0.3",
]
